<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.ribamar.concrete">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_githublogo_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_githublogo_round"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activities.PullRequestActivity"></activity>
        <activity android:name=".view.activities.RepositoriesActivity" />
        <activity
            android:name=".view.activities.SplashActivity"
            android:theme="@style/Activity.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>